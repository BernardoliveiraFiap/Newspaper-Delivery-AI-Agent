"""LangChain agent orchestration.

TODO: build the agent that combines the LLM (see llm.py) with the Tavily
search tool (see tools.py), runs it over the user's request, and returns
a plain-language summary plus up to 5 source links.
"""
